// routes/users.js
const express = require("express");
const router = express.Router();

// User Preferences & Settings
// router.get("/preferences", authenticateToken /* Get user preferences */);
// router.patch(
//   "/preferences",
//   authenticateToken /* Update favorite genres, etc. */
// );
// router.get("/activity", authenticateToken /* Get user activity history */);

module.exports = router;
